from abc import ABC


class BaseModuleModel(ABC):
    """
    A simple base class for data models. In a real application, this might
    implement an observer pattern or other features, but for now, it's a
    conceptual placeholder and data container.
    """
    pass